# Pending Safe Tasks (Next Move)

These items are intentionally deferred and non-blocking for immediate GitHub upload.

1. Run a second manual review over imported historical logs under `exports/` and `recovery/`.
2. Decide whether to keep or archive bulky exported raw artifacts outside the main repo history.
3. Add CI secret-scanning (for example, Gitleaks or GitHub Advanced Security) after repository creation.
4. Replace any remaining credential-like placeholders in docs/scripts with neutral examples if desired.
5. Install GitHub CLI (`gh`) or configure remote manually, then push `main` from `/home/shane/PsyNova`.
