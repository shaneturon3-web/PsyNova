# Decision Log

## 2026-04-25

- Adopted vertical-first execution order (booking -> session -> follow-up).
- Kept runtime API contracts unchanged while expanding frontend demo workflows.
- Introduced provider-agnostic virtual session strategy with fallback chain.
- Added explicit license-governance statuses to prevent unverified asset usage.
- Chose upsert-only Notion sync behavior (never delete remote content automatically).
