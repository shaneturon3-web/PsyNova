/** Placeholder for CLI tool / binary benchmarks. */
export async function benchmarkToolStub(name: string): Promise<{ name: string; note: string }> {
  return { name, note: 'tool_adapter: wire version --json + capability flags in v1.1' };
}
