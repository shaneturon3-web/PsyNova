# [MOCKUP PURPOSE ONLY - NOT REAL DATA]
DB_NAME=psynova
DB_USER=psynova
DB_PASSWORD=psynova_password_change_me
DB_PORT=5432

WP_DB_NAME=psynova_wp
WP_DB_USER=psynova_wp
WP_DB_PASSWORD=psynova_wp_password_change_me
WP_DB_ROOT_PASSWORD=psynova_wp_root_change_me
WP_DB_PORT=3306
WP_PORT=8080
WP_SITE_URL=http://localhost:8080
WP_SITE_TITLE="PsyNova Virtual Clinic"
WP_ADMIN_USER=psynova_admin
WP_ADMIN_PASSWORD=psynova_admin_change_me
WP_ADMIN_EMAIL=admin@psynova.local

# Docker Compose (optional): set to always include the backend container (binds host :3000).
# Leave unset if you run the API locally with `cd backend && npm run start:dev` — one owner of :3000.
# COMPOSE_PROFILES=prod
# (equivalent: COMPOSE_PROFILES=with-api)
