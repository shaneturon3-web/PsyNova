{
  "name": "psynova-staging-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker reverse-proxy that fronts the PsyNova cloudflared quick-tunnel with a stable *.workers.dev URL.",
  "scripts": {
    "deploy": "wrangler deploy",
    "redeploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "delete": "wrangler delete"
  },
  "devDependencies": {
    "wrangler": "^3.95.0",
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.0"
  }
}
