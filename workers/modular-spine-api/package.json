{
  "name": "modular-spine-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute psynova-edge-db --local --file=./migrations/0001_core.sql",
    "db:migrate:remote": "wrangler d1 execute psynova-edge-db --remote --file=./migrations/0001_core.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.19.0"
  }
}
